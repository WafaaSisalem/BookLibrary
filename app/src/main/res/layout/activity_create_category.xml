<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".mainscreens.CreateCategoryActivity">

    <EditText
        android:id="@+id/category_name_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:drawablePadding="15dp"
        android:drawableStart="@drawable/create_category_icon_24"
        android:hint="@string/category_name"
        android:inputType="text"
        app:layout_constraintTop_toTopOf="parent"
        />

    <Button
        android:id="@+id/create_new_cat_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:text="@string/create"
        app:layout_constraintEnd_toEndOf="@+id/category_name_edit_text"
        app:layout_constraintStart_toStartOf="@+id/category_name_edit_text"
        app:layout_constraintTop_toBottomOf="@+id/category_name_edit_text"
        />

</androidx.constraintlayout.widget.ConstraintLayout>